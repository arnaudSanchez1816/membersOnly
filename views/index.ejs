<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/stylesheets/style.css" />
    </head>
    <body>
        <h1><%= title %></h1>
        <% if (!locals.user) { %>
          <a href="/sign-in">Sign in</a>
          <a href="/sign-up">Sign up</a>
        <% } else { %>
          <p>
            Hello <%= locals.user.firstName %> <%= locals.user.lastName %> !
          </p>
          <a href="/sign-out">Sign out</a>
        <% } %>
    </body>
</html>
